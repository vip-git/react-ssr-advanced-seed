type Query {
    get{{pascalCase moduleName}}: [{{pascalCase moduleName}}]
    {{moduleName}}(id: ID!): {{pascalCase moduleName}}
}

type Mutation {
    create{{pascalCase moduleName}}(message: String, groupId: Int, ownerId: Int, date: String): {{pascalCase moduleName}}
    update{{pascalCase moduleName}}(
        id: ID!
        message: String
        groupId: Int
        ownerId: Int
        date: String
    ): {{pascalCase moduleName}}
}

type Subscription {
    {{moduleName}}Recieved: {{pascalCase moduleName}}
}

type {{pascalCase moduleName}} {
    id: Int
    groupId: Int
    ownerId: Int
    message: String
    type: String
    date: String
}