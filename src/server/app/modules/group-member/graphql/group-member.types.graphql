type Query {
    getGroupMember: [GroupMember]
    GroupMember(id: ID!): GroupMember
}

type Mutation {
    createGroupMember(memberId: Int, groupId: Int, date: String): GroupMember
    updateGroupMember(
        id: ID!
        ## Autogenerated GraphQL Model Update Definition ##
		memberId: Int
		groupId: Int
		date: String
    ): GroupMember
}

type Subscription {
    GroupMemberRecieved: GroupMember
}

type GroupMember {
    id: Int
    ## Autogenerated GraphQL Model Definition ##
	memberId: Int
    member: Profile
	groupId: Int
	date: String
}

input InputGroupMember {
	memberId: Int
	groupId: Int
	date: String
}